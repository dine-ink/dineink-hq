VITE_API_URL=https://dineink-backend.onrender.com
# PLACEHOLDER — this is the dev-pinned extension ID (from manifest.json's
# "key", only present in --mode development builds). Production builds ship
# without a pinned key, so Chrome Web Store assigns the real ID on publish.
# Replace this with that real published ID once known.
VITE_PROCUREMENT_EXTENSION_ID=ojfhfcaaohpjeeimplipeedknjcpbfhh